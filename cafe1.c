#include <stdio.h>
int main()
{
          int a=0, cont;
          
          if(a<1)
           {
            for(cont=1;cont<=100;cont++)
            printf("\n%i", cont);
           }
       system("pause");
       return 0;
}
